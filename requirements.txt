# --- Coeur MVP (obligatoire) ---
pandas>=2.0
openpyxl>=3.1
scikit-learn>=1.3
PyYAML>=6.0
python-dotenv>=1.0

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.0

# --- Interface de test metier ---
streamlit>=1.30

# --- Tests ---
pytest>=8.0

# --- Lecture de la recette PPTX (optionnel, inspection) ---
python-pptx>=0.6

# --- Optionnel: embeddings multilingues (ameliore le retrieval) ---
# Decommenter si vous voulez les embeddings (telechargement de modele requis).
# sentence-transformers>=2.6

# --- Optionnel: LLM externe (jamais requis, active via .env) ---
# openai>=1.30
# anthropic>=0.34
